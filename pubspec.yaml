name: mobile_engineer_interview
description: Mobile enginner interview exam

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^2.2.0
  flutter_riverpod: ^1.0.3
  faker: ^2.0.0
  hive: ^2.0.5
  hive_flutter:
  basic_utils: ^4.0.0
  sprintf: ^6.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  hive_generator:
  build_runner:

flutter:
  uses-material-design: true

